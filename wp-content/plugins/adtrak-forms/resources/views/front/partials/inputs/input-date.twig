<input 
	id="acpf-{{ fields.name }}"
	name="acpf-{{ fields.name }}"
	type="text"
	data-parsley-trigger="change"
	placeholder="{{ fields.placeholder }}"
	{% if (fields.required) %}required{% endif %}
	class="{{ fields.classes }} datepicker"
	{% if (fields.required) %}required{% endif %}
>